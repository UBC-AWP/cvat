metadata:
  name: pth-dschoerk-transt
  namespace: cvat
  annotations:
    name: TransT
    type: tracker
    spec:

spec:
  description: Fast Online Object Tracking and Segmentation
  runtime: 'python:3.10'
  handler: main:handler
  eventTimeout: 30s
  env:
    - name: PYTHONPATH
      value: /opt/nuclio/trans-t

  build:
    image: cvat.pth.dschoerk.transt:latest-gpu
    baseImage: ubuntu:22.04
    directives:
      preCopy:
        - kind: ENV
          value: NVIDIA_VISIBLE_DEVICES=all NVIDIA_DRIVER_CAPABILITIES=compute,utility
        - kind: RUN
          value: |-
            apt update \
              && apt install -y --no-install-recommends \
                git \
                ca-certificates \
                python-is-python3 \
                python3 \
                python3-pip \
              && rm -rf /var/lib/apt/lists/*
        - kind: RUN
          value: |-
            pip install \
              jsonpickle opencv-python-headless numpy==1.26.4 \
              torch torchvision \
              --extra-index-url https://download.pytorch.org/whl/cu128 \
              --no-cache-dir
        - kind: WORKDIR
          value: /opt/nuclio
        - kind: RUN
          value: |-
            git clone --depth 1 --branch v1.0 https://github.com/dschoerk/TransT trans-t && \
            sed -i 's%if float(torchvision.__version__\[:3\]) < 0.7%if False%g' trans-t/util/misc.py && \
            sed -i 's%torch.load(path, map_location=%torch.load(path, weights_only=False, map_location=%g' trans-t/ltr/admin/loading.py && \
            sed -i 's%from torchvision.models.resnet import model_urls%model_urls = {}%g' trans-t/ltr/models/backbone/resnet.py && \
            sed -i 's%backbone_pretrained=True%backbone_pretrained=False%g' trans-t/ltr/models/tracking/transt.py
        - kind: ADD
          value: https://drive.google.com/uc?id=1Pq0sK-9jmbLAVtgB9-dPDc2pipCxYdM5 /transt.pth

  triggers:
    myHttpTrigger:
      numWorkers: 1
      kind: 'http'
      workerAvailabilityTimeoutMilliseconds: 10000
      attributes:
        # Set value from the calculation of tracking of 100 objects at the same time on a 4k image
        maxRequestBodySize: 268435456 # 256MB

  resources:
    limits:
      nvidia.com/gpu: 1

  platform:
    attributes:
      restartPolicy:
        name: always
        maximumRetryCount: 3
      mountMode: volume
